put bootstrap.ps1 on system drive
make it a powershell startup script (gpedit.msc > Computer Settings > Windows Settings > Scripts (Startup) > Powershell Scripts tab)
connect cd to do sysprep (you can use cdburner xp to create new iso and to alter the script)